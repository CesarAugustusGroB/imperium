[workspace]
resolver = "2"
members = ["crates/sim_core", "crates/imperium", "crates/imperium-mcp"]

[workspace.dependencies]
bevy = "0.18"
bevy_ecs = "0.18"

# Faster iterative dev builds: optimize deps, keep our crates debuggable.
[profile.dev.package."*"]
opt-level = 3
